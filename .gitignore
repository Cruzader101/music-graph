# Secrets
.env

# Data / cache (never in git — music-project.md)
cache.db
cache.db-journal
cache.db-wal
cache.db-shm
data/derived/
frontend/graph.json

# Logs
logs/

# Internal working docs (session hygiene, CLAUDE.md working agreement) — not for public repo
devlog.md
music-project.md

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Vendored libs are tracked (offline-safe frontend) — do not ignore frontend/vendor/
